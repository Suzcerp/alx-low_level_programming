#include "main.h"

#include <stdio.h>

/**
** main - check the code
** _isupper - a function that checks for uppercase character 
** Return: Always 0.
**/

int main(void)

{
	
	char c;
	
	if ((c >= 'A') && (c <= 'Z'))
		
		return (1);
	else
		
	return (0);
	
}
